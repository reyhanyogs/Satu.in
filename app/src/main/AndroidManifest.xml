<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.satuin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Satuin">
        <activity
            android:name=".HomepagePenjual"
            android:theme="@style/ProfilUsaha"/>
        <activity
            android:name=".ProfilUsaha"
            android:theme="@style/HomepagePenjual" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
            </activity>
        <activity
            android:name=".Register"
            android:theme="@style/Register" />
        <activity
            android:name=".GettingStarted2"
            android:theme="@style/GettingStarted2" />
        <activity
            android:name=".Login"
            android:theme="@style/Login" />
        <activity
            android:name=".GettingStarted"
            android:theme="@style/GettingStarted" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/MainActivity" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>